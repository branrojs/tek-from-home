
  <div class="container white-body">
    <div class="row"> 
    <% if !current_user %>
      <div class="col-lg-12 col-md-offset-4">
        <%= image_tag "logo2.png", class: 'welcomepic'%>
      </div>
      <div class="col-lg-12">
        <div class="well well-lg">
          <h2 class="text-center">CheKNow's Description</h2>
          <h4>CheKNow born as a WFH assistance tool to help the managers to get access to reports of work from his employees
          while they are working from home.
          </h4>
          <h4>
            This tool have a module for the employees as well, they can have a control of the cases they are working and, at the end 
            of the shift, they can send a report to the manager.
          </h4>
          <%= link_to('Sign In', new_user_session_path, class: 'btn btn-primary', title: 'Sign In') %>
          <%= link_to('Sign up', new_user_registration_path, class: 'btn btn-primary', title: 'Sign In') %>
        </div>
    <% else %>
      <div class="well col-lg-10 text-center">
        <h1>Welcome <%= current_user.name %> </h1>
        <% if !current_user.position.nil? && current_user.position.name == "Manager" %>
          <h3>Manager!<small> You have <%= @reports.count %> new reports unread</small></h2>
        <% end %>
        <div >
          <h2>When you feel ready to start, push the button!</h1>
          <%= button_to current_user.status ? "Check-out" :"Check-in"  , { action: "update_status" }, class: current_user.status ? "btn btn-lg btn-danger" : "btn btn-lg btn-success" %>
          <h3><small>Your current state is <strong><%= current_user.status ? "Active" :"Inactive" %></strong></small></h3>
          
          <p>Remember, if this is the first time here, you need to assing a team and a position at <%= link_to current_user.name, edit_user_registration_path %></p>
        </div>
    <% end %>
 















