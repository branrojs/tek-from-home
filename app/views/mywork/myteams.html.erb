<h1> <%= @myteam.name %></h1>
<table class="table table-bordered">
  <thead>
    <tr>
      <th colspan="2">Roster</th>
    </tr>
  </thead>
  <tbody>
    <%= render 'myteam', obj: @myteam.users %>
  </tbody>
</table>
<%#= link_to t('.new', :default => t("helpers.links.new")),
            new_case_path,
            :class => 'btn btn-primary' %>